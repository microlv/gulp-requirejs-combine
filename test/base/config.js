'use strict';

var config = {
  baseUrl: '.',
  paths: {
    jsonp: 'ajax/jsonp',
    load: 'ajax/load',
    xhr: 'ajax/xhr',
    nonce: 'ajax/var/nonce',
    rquery: 'ajax/var/rquery'
  }
};

module.exports = config;