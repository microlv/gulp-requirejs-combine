'use strict';

var config = {
  baseUrl: './demo',
  paths: {
    jsonp: 'ajax/jsonp',
    load: 'ajax/load',
    xhr: 'ajax/xhr',
    parseHTML: 'core/parseHTML',
    ajax: 'event/ajax',
    alias: 'event/alias',
    nonce: 'ajax/var/nonce',
    rquery: 'ajax/var/rquery'
  },
  useStrict: true
};

module.exports = config;